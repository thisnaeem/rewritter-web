import type { Metadata } from "next";
import { Figtree } from "next/font/google";
import "./globals.css";

const figtree = Figtree({
  variable: "--font-figtree",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "Rewritter - AI-Powered Grammar & Summarization Tool",
  description: "Transform your writing with AI-powered grammar correction and intelligent summarization. Available as a web app and Chrome extension.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${figtree.variable} font-figtree antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
